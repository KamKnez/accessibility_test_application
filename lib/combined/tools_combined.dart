import 'package:flutter/material.dart';

class ToolsCombinedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Tools Combined Page',
        style: Theme.of(context).textTheme.headlineMedium,
      ),
    );
  }
}
